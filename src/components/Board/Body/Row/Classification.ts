class Classification {
    rowId: string;
    name: string;

    constructor(rowId: string, name: string) {
        this.rowId = rowId;
        this.name = name;
    }
}

export default Classification;
